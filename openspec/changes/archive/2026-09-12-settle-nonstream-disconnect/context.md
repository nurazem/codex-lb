The final-response collector previously awaited upstream SSE aggregation without observing the ASGI receive channel. A downstream disconnect could leave collection running until the upstream terminal or timeout. The response collection owner now races collection with one disconnect watcher, cancels collection once, and joins its existing service cleanup. Terminal collection wins a simultaneous disconnect; the observer never owns reservations or retries.

Validation: 217 local response-contract, reservation, collection, and route tests passed. Four installed controls passed in the candidate image and running container, with no network or paid upstream requests. Image d896d70fa9c117787d8593e92de19648b7b2b50db75561fe2d6c61b555aa3e40 preserves the prior image layers; only the collection function, its import, and the new helper changed. Public and local health checks passed. Existing temporary diagnostic files were retained outside the replaced container.
