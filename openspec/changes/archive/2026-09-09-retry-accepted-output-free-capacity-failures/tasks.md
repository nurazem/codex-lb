# Tasks

## 1. Reproduction

- [x] 1.1 Add HTTP bridge integration tests where upstream accepts the turn (`response.created` + `response.in_progress`) and fails output-free with a capacity `error` or an abrupt close, asserting a retry on another account and exactly one `response.created` whose id the `response.completed` carries (fail on `main`).
- [x] 1.2 Add the same WebSocket integration tests, plus sequenced variants proving the existing fail-closed sequence contract is kept (capacity terminal forwarded unchanged, abrupt close recorded as `stream_incomplete` with a 1011 downstream close, no replacement connect).

## 2. Classification and Eligibility

- [x] 2.1 Add the accepted-lifecycle-only predicate to `support.py` and generalize `_websocket_request_can_replay_before_visible_output` from created-only to lifecycle-only; a downstream sequence watermark keeps refusing the replay as on `main` (only the created-only `generate: false` prewarm exception survives).
- [x] 2.2 Add `http_bridge/accepted_replay.py` with the accepted capacity classifier (capacity codes or the selected-model capacity message; refuses other pending requests, consumed replays, anchors without a retry-safe fresh payload, terminals naming another response, and terminals reporting output items or billed output/reasoning tokens) and delegate accepted states from `_websocket_precreated_retry_error_code`.

## 3. Single-Lifecycle Replay

- [x] 3.1 Add `suppress_next_in_progress_downstream` and suppress the replay's `response.in_progress` on both surfaces. The WebSocket sequence-regression guard keeps its created-only exemption: a sequenced prelude is never replayed (5.15), so no suppressed `response.in_progress` can meet that guard.
- [x] 3.2 Stage accepted replays before `response_id` is cleared at the bridge capacity-wait and transparent-code branches and at the WebSocket transparent-code branch; preserve an already captured identity in `_prepare_websocket_request_state_for_visible_output_replay`.
- [x] 3.3 Re-claim the session response-create gate without waiting (bridge terminal and transport-close paths), mark work admission for re-acquisition, and exclude the failing account on the WebSocket surface.
- [x] 3.4 Let only terminal transport messages (close/error) replay an accepted turn in the bridge relay loop.

## 4. Verification

- [x] 4.1 Unit tests for the classifier, eligibility predicate, replay preparation, staging, gate re-claim, binary-frame guard, and mutants (output seen, billed output, other pending request, busy gate, second failure, quota codes, foreign response id).
- [x] 4.2 Run ruff, the proxy architecture check, the touched bridge/websocket unit and integration suites, and strict OpenSpec validation for this change.

## 5. Review Follow-ups

- [x] 5.1 Record model output on the direct websocket relay (`upstream_model_output_seen` for the shared `_MODEL_OUTPUT_EVENT_TYPES`) so an accepted turn whose upstream skipped `response.in_progress` is never replayed after a forwarded output item; mutant tests cover the capacity-error and abrupt-close paths.
- [x] 5.2 Leave `terminal_settlement_phase` untouched when staging a bridge replay so a failed replay followed by an abort still settles the API-key reservation through the shielded #1594 path; unit tests for the preserved claim and the abort settlement.
- [x] 5.3 Mark shared work admission for re-acquisition whenever the session create gate is re-claimed for an accepted replay, so the bridge transport-close path re-enters the configured work limit like the terminal-error path; unit tests on the claim helper and the transport-close replay.
- [x] 5.4 Emit websocket idle keepalives under the client-visible response id (`_websocket_downstream_response_id`, bridge parity), including the staged phase before the replay's `response.created` arrives; unit test.
- [x] 5.5 Make the websocket two-account failover harness honor `excluded_account_ids` and the hard owner requirement so a missing exclusion or a contradicted owner pin fails the integration tests; add anchored (proxy-injected `previous_response_id` + retry-safe fresh body) accepted cases for the capacity-terminal and transport-close paths.
- [x] 5.6 Reconcile the owner pin when the visible-output replay swaps in the fresh body (shared with `_install_verified_fresh_replay`) and decide the accepted-replay exclusion with `_websocket_accepted_replay_can_switch_account`, which mirrors the connect-side owner requirements; unit tests for the prepare, the predicate, and the transport-end replay.
- [x] 5.7 Report accepted `model_at_capacity` terminals as `server_is_overloaded` (pre-created classifier parity) so accepted replays stay under a transparent replay code on the websocket surface; classifier, text-path, and integration tests over both codes.
- [x] 5.8 Defer the failing account's health write behind API-key settlement for accepted websocket replays (`_record_or_defer_websocket_accepted_replay_health`); call-order unit test.
- [x] 5.9 Gate the bridge's bounded clean-close retry off accepted lifecycles so an accepted turn is re-sent exactly once; predicate and bridge retry unit tests, spec wording.
- [x] 5.10 Keep accepted lifecycles off the pre-created anchored owner-switch branches on the direct websocket capacity path: the capacity branch runs the owner-switch prep where the replay is staged, and an anchored turn the prep cannot move (client-supplied anchor, account-bound fresh body) is re-sent as the anchored body to the owner that accepted it with the health write deferred behind API-key settlement, instead of failing closed as `previous_response_owner_unavailable`; unit tests for both shapes (unkeyed and keyed) and a client-anchored integration test.
- [x] 5.11 Keep the turn-state owner pin (`preferred_account_id` of a `codex_session_source == "turn_state"` request) through the fresh-body install so the exclusion predicate sees the owner the session loop re-resolves and the connect hard-requires; the pre-created `retry_safe_owner_replay` branch gates its exclusion on the same predicate. Unit tests for both install paths, both accepted websocket paths, and the pre-created branch; `x-codex-turn-state` integration tests on the capacity-terminal and transport-close paths.
- [x] 5.12 Let the bridge's transparent-code (no capacity message) branch stage an accepted anchored follow-up with a proxy-injected anchor and a retry-safe fresh body (`_http_bridge_accepted_anchored_replay_candidate`), matching the capacity-message wait branch; a client-supplied anchor keeps forwarding the terminal because the pre-created retry refuses transport-only full resends. Unit tests for both shapes.
- [x] 5.13 Release the anchor owner pin on the visible-output replay only for a proxy-injected anchor (`_install_fresh_replay_body(release_owner_pin=...)`); a client-supplied anchor keeps `replay_required_account_id` / `preferred_account_id` as on `main`, so the transport-close replay of an accepted client-anchored turn reconnects to its owner instead of moving. Unit tests for the prepare and the transport-end replay, integration test on the transport-close path.
- [x] 5.14 Refuse the bridge transport-close accepted replay while another response shares the upstream socket (mirror of the terminal path's other-pending guard, before the gate claim) so both requests fail closed with `stream_incomplete` as on `main`; unit tests on the retry and on the relay loop.
- [x] 5.15 Respect the existing requirement "Direct WebSocket replay never mixes numeric response sequences": the accepted capacity classifier and the transport-close predicate refuse any request whose forwarded prelude carried a finite `sequence_number` (only the created-only `generate: false` prewarm exception from `main` survives), and the websocket sequence-regression guard is back to its created-only exemption. Unit tests for the predicate, the classifier, the transport-end pop, and the text path; integration tests for the sequenced capacity terminal and the sequenced abrupt close.
- [x] 5.16 Apply the client-anchor exclusion (`_http_bridge_accepted_capacity_retry_allowed`) before the bridge capacity-message wait branch reserves or stages an accepted request, so a client-supplied anchor keeps its raw capacity terminal instead of a synthetic `stream_incomplete`; capacity-message twin of the transparent-code test plus a proxy-injected guard test.
- [x] 5.17 Record the terminal settlement claim when the capacity-message wait branch relinquishes pending ownership without a successful replay (`_relinquish_http_bridge_capacity_wait_ownership`, both the refused/failed-replay and the busy-gate shapes), and correct the staging docstring; abort tests for both shapes.
- [x] 5.18 Keep the pre-response startup wait un-signalled for accepted propagated-error streams on both signal sites (the wait branch and the capacity sleep, `signal_startup_wait=False`); a unit test pins the per-request and propagated startup events ready across the accepted replay so dropping either guard fails.
- [x] 5.19 Re-evaluate the direct websocket other-pending guard under the pending lock right before the replay classifiers run: the snapshot taken when the terminal popped the accepted request predates the thread-affinity write that follows, and the sender can admit and send a younger `response.create` on the same socket during that await because the accepted request released the session gate at `response.created`. Deciding on the stale snapshot retired the shared socket under the younger turn (no terminal, gate wedged) and blocked the replay behind it. Text-path and relay-level unit tests drive the interleaving through the real sticky-session write.
- [x] 5.20 Refuse the direct websocket accepted-replay exclusion whenever the request's affinity may resolve to a hard sticky owner the state does not carry (`_websocket_affinity_may_resolve_hard_owner`: `CODEX_SESSION` kind or a raw legacy compatibility lookup; decided by `_websocket_accepted_replay_may_exclude_account` at both accepted sites, the pre-created owner-switch branch keeps the pin-only predicate), so a bare-session turn whose raw row names a hard owner reconnects to that owner as on `main` instead of failing every re-selection with `hard_affinity_saturated`. Predicate, transport-end, and text-path unit tests; the two-account failover harness models the raw hard row and integration tests cover the capacity-terminal and transport-close paths; the anchored account-switch integration tests run without a Codex session header, with session-header twins asserting the owner reconnect.
- [x] 5.21 Pin and document the HTTP bridge transport-close shape with an accepted request and a pre-created gate-holding sibling: both fail closed promptly with `stream_incomplete` (`main` retried the sibling alone and stranded the accepted request); retry-level and relay-level unit tests, spec scenario and proposal limitation.
