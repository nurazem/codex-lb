## 1. Pending lifecycle owner readiness

- [x] 1.1 Extend the existing real-route owner regression for background JSON acknowledgements and later in-progress SSE, and reproduce missing owner resolution before production edits using the isolated test guard.
- [x] 1.2 Complete lifecycle and relay classification, then pass the expanded regression and existing parsing, WebSocket, and bridge coverage.

## 2. Verification

- [x] 2.1 Verify the implementation against the delta, synchronize stable context and requirements, and pass strict OpenSpec validation before archive.
