# Tasks

## 1. Fix A — replica-local burst cooldown for unbound selection

- [x] 1.1 (A1) `app/modules/proxy/_load_balancer/types.py`: add `RuntimeState.burst_backoff_until: float | None = None` next to the `overload_*` fields, commented as replica-local, never persisted, set by a code-less upstream HTTP 429 burst/concurrency rejection.
- [x] 1.2 (A2) `app/modules/proxy/_load_balancer/overload_backoff.py`: add `BURST_BACKOFF_DEFAULT_SECONDS = 5.0` and `BURST_BACKOFF_MAX_SECONDS = 30.0`; `overload_backoff_active` returns true while `now` is before either `overload_backoff_until` or `burst_backoff_until` (`overload_isolation_active` untouched); add `record_burst_rejection_locked(runtime, now, *, retry_after_seconds) -> float` (`burst_backoff_until = max(existing, now + clamp(retry_after or default, default..max))`, returns applied seconds) and `async record_upstream_burst_rejection(balancer, account, *, retry_after_seconds=None, redact_account_id=False)` modelled on `record_upstream_overload` (same `_runtime` guard, per-account lock, `balancer._clock.time()`, one `WARNING` `Account burst backoff engaged account_id=%s backoff_seconds=%.1f retry_after_seconds=%s http_status=429`). Never write `overload_backoff_until` or the overload rejection window. Update the module docstring.
- [x] 1.3 (A3) `app/modules/proxy/_service/streaming/helpers.py::_handle_stream_error`: add keyword-only `retry_after_seconds: float | None = None` (and to the protocol stub in `app/modules/proxy/_service/support.py`); in the final `else` branch after `record_error`, alongside the `UPSTREAM_OVERLOAD_CODES` hook, add `elif http_status == 429: await record_upstream_burst_rejection(...)` with `redact_account_id=privacy_policy.redacts_sensitive_details`. Keyed on the HTTP status, not the code; rate-limit/quota classes return earlier so coded 429s keep `mark_rate_limit`. Add pure `is_upstream_burst_rejection(*, failure_class, http_status) -> bool` to `app/modules/proxy/helpers.py` without changing `classify_upstream_failure` outputs.
- [x] 1.4 (A4) `app/modules/proxy/service.py`: pass `retry_after_seconds=exc.retry_after_seconds` at the two `_handle_stream_error` call sites only if it costs at most two net lines against the 2600-line ceiling; otherwise skip and record the omission in the report.

## 2. Fix B — owner-bound burst 429 retries the same owner

- [x] 2.1 (B1) `app/core/balancer/logic.py`: extend `FailoverAction` with `"retry_same_account"`; `failover_decision(*, failure_class, downstream_visible, candidates_remaining, owner_bound=False, same_account_retry_available=False)` returns `surface` when downstream-visible, `retry_same_account` / `surface` when owner-bound (never `failover_next`), and the existing result otherwise; existing callers (`websocket/mixin.py`, `compact.py`, `retry.py`) compile via the defaults. Add `BURST_SAME_ACCOUNT_MAX_RETRIES = 3`, `BURST_SAME_ACCOUNT_BASE_SECONDS = 1.0`, `BURST_SAME_ACCOUNT_MAX_WAIT_SECONDS = 10.0` and pure `burst_same_account_backoff_seconds(retry_index, *, retry_after_seconds) = min(MAX_WAIT, max(retry_after or 0, BASE * 2**(retry_index-1)))` (no time/random seams; jitter only through an injectable RNG or omitted).
- [x] 2.2 (B2) `app/modules/proxy/_service/streaming/retry.py` pre-visible failover block: derive `owner_bound` from the loop's existing continuity inputs (payload replay binding to this account, required preferred account, file-pin owner, turn-state owner, single-account routing) by reusing/refactoring the existing can-try-other-account predicate; `burst = is_upstream_burst_rejection(...)` on the exception's status; per-account `burst_same_account_retries` counter reset when a different account is selected; `same_account_retry_available = burst and counter < MAX and remaining budget > 0 and not settlement.downstream_visible`; call `failover_decision` with both flags and log `Failover decision ... action=<returned action>`. On `retry_same_account`: keep the account (no exclusion, keep `payload_replay_required_account_id`, keep the tracked stream lease so the redispatch stays inside the admitted slot), engage the burst cooldown directly via `record_upstream_burst_rejection` with `retry_after_seconds` (no per-retry `record_error`), increment the counter, compute the delay, wait through `_iter_account_capacity_recovery_wait(..., recovery_sleep_seconds=delay, remaining_budget_seconds=..., emit_keepalives=not propagate_http_errors, stage="burst_backoff", scheduler=scheduler, clock=clock)` yielding its events, then `continue` the same-account loop (fall through to the surface path when the budget was spent during the wait). On `surface`: existing health write (exactly once; the surfaced exception is marked so the outer terminal handler skips it) + raise of the original upstream 429, setting `retry_after_seconds=5` on the propagated `ProxyResponseError` when upstream gave none.
- [x] 2.3 (B3) Apply the same `owner_bound` / `same_account_retry_available` / honest-log treatment to the post-refresh failover block (`phase=post_refresh`) through a shared helper rather than a duplicated predicate.
- [x] 2.4 (B4) Thread `retry_after_seconds` through `_handle_or_defer_keyed_stream_health` and the `pending_post_refresh_transient_penalties` tuple into `_handle_stream_error(..., retry_after_seconds=...)` against the fix A signature. When the keyed path defers a burst rejection, engage the cooldown eagerly and queue the tuple with `burst_cooldown_recorded=True` so the deferred `_handle_stream_error` skips the burst hook.
- [x] 2.6 (review) `app/modules/proxy/api.py::_wait_for_first_stream_probe`: keep watching the wait marker during the bounded post-ready window so consecutive burst waits keep the headers held instead of expiring the window and committing 200/SSE.
- [x] 2.5 (B5) Confirm a non-owner-bound burst 429 still yields `failover_next` and that the next selection is steered by the fix A cooldown.

## 3. Tests

- [x] 3.1 `tests/unit/test_overload_backoff.py`: burst cooldown engages on a code-less 429 through `_handle_stream_error` (`retry_after_seconds` absent -> 5 s; present -> clamped to 5..30 s; extend-never-shorten), `overload_backoff_active` honors either deadline, `overload_isolation_active` and the overload window are untouched, coded 429 does not engage it; keep `test_handle_stream_error_feeds_the_overload_window_for_overload_codes_only` green (make its assertion overload-window specific if needed).
- [x] 3.2 Balancer selection tests: unbound selection skips the cooling account while a sibling exists, a lone candidate still serves, an established `prompt_cache` owner and a hard continuity owner are kept, persisted status / `reset_at` / `blocked_at` untouched.
- [x] 3.3 `tests/unit/test_failover_foundation.py`: `failover_decision` owner-bound matrix (`retry_same_account` / `surface`, never `failover_next`; downstream-visible always `surface`), `burst_same_account_backoff_seconds` schedule (1 s, 2 s, 4 s; `Retry-After` floor; 10 s cap), `is_upstream_burst_rejection`; existing classification pins stay green.
- [x] 3.4 `tests/integration/test_proxy_transient_retry.py`: owner-bound (reasoning-item payload) code-less 429 retried on the same account and succeeding; with native Codex headers: four rejections surface HTTP 429 with the original body and `Retry-After: 5` (`Retry-After: 3` preserved when upstream sent it, waits 3/3/4), `Failover decision` log reports `retry_same_account` x3 then `surface`, the successful retry leaks no `waiting_for_account_capacity` keepalive; coded 429 (`test_stream_connect_phase_429_usage_limit_transparent_failover`) still persists `RATE_LIMITED`.
- [x] 3.6 (review) Unit coverage for the review findings: keyed same-account retry (API key + reservation) engages the cooldown before the first sleep and leaves no penalty after `record_success`; keyed `failover_next` stamps the cooldown at rejection time and the deferred flush does not extend it; exhausted owner-bound burst records exactly one `record_error`; single-account routing yields `retry_same_account`; post-refresh `retry_same_account` re-selects the same owner (and surfaces with `Retry-After` when the owner is gone); `_wait_for_first_stream_probe` re-arms on a newer wait marker during the post-ready window; `_handle_stream_error(burst_cooldown_recorded=True)` skips the burst hook.
- [x] 3.5 Keep green: `tests/unit/test_proxy_utils.py -k "test_rate_limit_still_marks_rate_limit or test_genuine_upstream_error_still_penalizes_account"`, `tests/unit/test_websocket_transport_fallback.py`.

## 4. Validation

- [x] 4.1 `ruff check`, `ruff format --check`, `ty check`.
- [x] 4.2 `scripts/check_proxy_architecture.py` (service.py <= 2600, load_balancer.py <= 3021, streaming/mixin.py <= 1100, http_bridge/mixin.py <= 2436, `select_account` span <= 527), `scripts/check_proxy_timing_seams.py` (zero raw sleeps/clocks in `retry.py` and `streaming/helpers.py`), `scripts/check_cancellation_safety.py`, `scripts/check_settings_tiers.py` (ratchet stays 130, no new `CODEX_LB_*` setting).
- [x] 4.3 `openspec validate backoff-upstream-burst-429-on-http-stream --strict` and `openspec validate --specs`.
