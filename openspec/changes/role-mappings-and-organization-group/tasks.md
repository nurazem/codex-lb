## 1. Backend

- [x] 1.1 `DashboardRoleMapping` in `app/db/models.py` (`provider`, `provider_key`, `claim_name`, `claim_value`, `role_id` FK RESTRICT, `priority`, `UNIQUE(provider, provider_key, priority)`) and the Alembic revision on top of `20260910_010000_add_dashboard_auth_providers` (inspector guards, fresh-`Column` factory, the unique constraint declared inline for SQLite, mirrored downgrade).
- [x] 1.2 Settings: `dashboard_auth_proxy_groups_header` (default `Remote-Groups`, `normalize_dashboard_auth_proxy_header` before-validator, refuses to equal `dashboard_auth_proxy_header`), `SETTING_TIERS` T1 entry, `.env.example` 46 → 47, `[settings_fields]` 95 → 96 with a history line, regenerated `docs/reference/settings.md`.
- [x] 1.3 Groups on the request path: `DashboardRequestAuth.groups` from the groups header (single occurrence, comma-separated, trimmed, deduplicated, bounded, empty on a duplicated header), through `resolve_trusted_header_request` into `ExternalIdentity.groups`, snapshotted on `dashboard_identities.groups_json` when it changes.
- [x] 1.4 Evaluation in `IdentityResolver` (the last-admin count read behind `acquire_write_intent()`, in the transaction the demotion commits): highest-priority single winner over `groups`/`email_domain`; `_provision` consults rules before `unknown_identity_role_id`; `_seen` re-evaluates only `role_source=mapping` accounts of a provider with ≥1 rule and `skip_role_sync=false`, writes only on change, demotes to `no_match_role_id` (`NULL` disables) with `role_demoted_no_mapping`, pins to `manual` with `role_source_overridden reason=last_admin_protected` when the last admin would be lost, and audits `user_role_changed source=mapping`.
- [x] 1.5 `app/modules/role_mappings/` (`api`, `service`, `repository`, `schemas`): list/create/patch/delete plus `PUT /api/role-mappings/order`, `security:write` router gate, `_require_account()` 409 `admin_account_required` on every route including the reads, `resolve_assignable_role` + `assert_can_delegate` on the role a write hands out *and* on the role an edited, deleted or moved rule already hands out, dense server-owned priorities renumbered in one transaction whose unique violation becomes `409 order_stale`, `409 mapping_exists` / `409 mapping_limit_reached` / `404 mapping_not_found` / `422 unknown_claim`, audit `role_mapping_changed` naming the role on every variant, registry invalidation; context in `app/dependencies.py`, router in `app/main.py`.
- [x] 1.7 `GET /api/role-mappings/assignable-roles`: the roles this `security:write` caller may hand out, filtered by the shared `role_assignable_to_users` predicate and `assert_can_delegate`, so the organisation cards do not need the `users:manage` roles list.
- [x] 1.6 `force` on `PATCH /api/dashboard-users/{id}`: `409 role_managed_externally` for a `role_source=mapping` account without it, `role_source → manual` and `role_source_overridden` with it; `_ERROR_MAP` entry; `count_role_mappings()` through the dashboard-auth repository so `access_summary.role_mappings` is real.

## 2. Frontend

- [x] 2.1 `AdvancedSettingsGroup` label props (defaults keep `settings.advanced.*` and the "Show advanced settings" name); `OrganisationSettingsGroup` rendered after it in `settings-page.tsx` for `security:write`; one-liner without the banned words, status summary when `accessSummary` reports something configured; `#organisation` deep link in `advanced-settings-deeplink.ts`.
- [x] 2.2 Reverse-proxy card: read-only header names with the `CODEX_LB_DASHBOARD_AUTH_PROXY_*` note, editable unknown-identity default, no-match role, `linkByEmail`, `skipRoleSync` through `PATCH /api/auth-providers/{id}`, neutral wording for solo installs.
- [x] 2.3 Group-to-role rules card: priority list with Move up / Move down (and native drag) writing one `PUT /api/role-mappings/order`, add/edit/delete, empty state and quick-add "everyone at `<domain>` as `<role>`", the re-evaluation warning before the first rule is saved, and the refused-sign-ins line with the `audit:read`-only sheet.
- [x] 2.4 Shared role picker (presets first with a lock icon, custom roles only when one exists, options limited to what the caller may hand out — from the session's `assignableRoleIds` on the People surfaces, from `GET /api/role-mappings/assignable-roles` inside the Organisation group) reused by the rules card, the invite dialog and the row role change; "Managed by company login" badge and the take-over action (`force: true`) in the People tab; `role_managed_externally` in `EXPLAINED_ERROR_CODES`.
- [x] 2.5 `features/organisation/{api,hooks,rules}.ts`, MSW handlers plus `EXPECTED_ENDPOINTS` entries for `/api/role-mappings`, `/api/role-mappings/order`, `/api/role-mappings/assignable-roles` and `/api/audit-logs`, factories for providers and mappings, i18n keys in en, ko and zh-CN.

## 3. Verification and docs

- [x] 3.1 Unit: priority renumbering, matcher (groups, email domain, case folding, no match, single winner), groups header parsing (duplicate header, bounds), settings validator (reserved header, equal to the identity header).
- [x] 3.2 Integration: the whole re-evaluation table — manual untouched, zero rules untouched (D10), `skip_role_sync`, demotion to `no_match_role_id`, `NULL` disables, `unknown_identity_role_id` change is JIT-only, last-admin pinning, one resolver run and no write when nothing changed; mapping CRUD and reorder including delegation on the rule's current role, the stale-order refusal a concurrent renumber gets, step-up, the attributable-account gate on the reads and the audit rows; `force` on the user PATCH; `access_summary.role_mappings`; the new routes in `EXPECTED_REQUIREMENTS` and `STEP_UP_GATED`.
- [x] 3.3 Frontend: collapsed Organisation group issues no enterprise request and mounts nothing; the one-liner matches `/\b(user|role|SSO|SCIM|IdP|RBAC)\b/i` nowhere; rules card empty state, quick-add, reorder, refused-sign-ins line with and without `audit:read`; reverse-proxy card read-only header names; role picker ordering and lock icon; a `security:write`-only session still naming and choosing a role; take-over flow; lint, typecheck, full vitest.
- [x] 3.4 `uv run ruff check . && uv run ruff format --check . && uv run ty check`; `openspec validate role-mappings-and-organization-group --strict`.
