## 1. Backend

- [ ] 1.1 `DashboardSettings.local_login_policy` (string, default `enabled`, `StrEnum`-validated) and the Alembic revision parented to the current single head (`20260911_010000_merge_pin_index_and_affinity_heads`) (idempotent inspector guard, fresh-`Column` factory, `op.batch_alter_table`, mirrored downgrade); thread it through the seven settings touch points (model, repository default and `update()` kwarg, `DashboardSettingsData`/`DashboardSettingsUpdateData` and the projection, response schema plus `SECURITY_SETTINGS_FIELDS`, request schema, API projection, merge block and `changed_fields`), and add it to `DashboardAuthSettingsProtocol`. No `Settings` field, no `SETTING_TIERS` entry, no `.env.example` line.
- [ ] 1.2 Qualification helpers next to `app/modules/dashboard_users/credentials.py`: the five-fact predicate (designation, active, admin preset, second factor, local password), `count_qualifying_break_glass(exclude_user_id=..., post_state=...)` on the users repository beside `count_active_admins`, and one `assert_break_glass_remains(...)` service function that refuses with `LastBreakGlassProtectedError` → `409 last_break_glass_protected` in `_ERROR_MAP`; serialise with `acquire_write_intent()` taken **before the path's first read** (`BEGIN IMMEDIATE` cannot run inside an open transaction) with no commit between the count and the write, and re-apply the count inside the guarded write exactly as `update_role_status_guarded` does for the last admin.
- [ ] 1.3 Call the guard from all eight sites: the role and status branches of `DashboardUsersService.update_user`, `delete_user`, the new `isBreakGlass` field on `DashboardUserUpdateRequest` (admin preset only, forces `role_source=manual`), `DashboardAuthService.disable_totp`, `DashboardUsersService.reset_totp`, the new shared `deactivate_user()` (pins and audits once on the resolver path, emits `scim_deprovision_refused` on refusal) called by the status change and by `IdentityResolver._demote`, and a documented no-op for custom-role delete-and-reassign (Phase 5b) recorded in the spec, not stubbed in code.
- [ ] 1.4 Sign-in: apply `local_login_policy` in `login_password` (refusal is byte-identical `401 invalid_credentials` + `login_failed reason=login_policy`), always require the second factor for a designated account, emit `break_glass_login` at `AuditSeverity.CRITICAL` from `verify_totp`, add the optional `bg` claim to the version-2 payload through `create_user_session`/`_create_user_session`/`verify_totp` and to `DashboardSessionState`, and report `break_glass_session` on the session response.
- [ ] 1.5 Replace `_password_fallback_principal` and `has_admin_fallback_session` in `app/core/auth/dependencies.py` with one policy-aware function that `_trusted_header_principal`, `validate_dashboard_session`, `_decorate_session_response` and `_trusted_header_session_response` all call, so the gate and the session response cannot disagree; widen `DashboardLoginHint.local_login` and `DashboardAccessSummary.local_login_policy` from their one-value `Literal`s and read the stored value in `login_hint()` and `access_summary()`.
- [ ] 1.6 Rate limiting: **two** buckets with distinct limiter types — the per-(address, username) budget at 8/60 s keyed on `normalize_username(username)`, and the coarse per-address ceiling at 60/60 s — both incremented by every attempt, of which **only the per-account one is cleared on success** (clearing the coarse bucket would let anyone holding a single valid account reset the endpoint's only ceiling between sprays); no account is exempt from either; the `username_required` audit guard reads the per-address bucket (the one ordinary logins increment); wire `DatabaseRateLimiter.cleanup()` into the leader pass of the sticky-session cleanup scheduler so failed-attempt rows age out.
- [ ] 1.7 Gates in the other direction: `break_glass_requires_totp` (409, body names the designated account) in `SettingsService.update_settings` on a tightening transition only, mirrored in `AuthProvidersService.update_provider` for the new `enabled` field on password-less providers, with `provider_enabled`/`provider_disabled` audit rows; both gates take the account write intent before their first read and hold it across their own commit; `login_policy_changed` on every policy change.
- [x] 1.8 `codex-lb admin` group in `app/cli.py` dispatching to a new `app/admin_cli.py`: `reset-password <username>` (getpass, never on argv, bumps `session_generation`, `--clear-two-factor` for a lost authenticator, re-opening `local_login_policy` when its own write would leave no account the policy admits), `local-login enable`, `disable-provider <id>`, `reset-login-policy`; `get_settings().database_url` → `to_sync_database_url()` → per-command engine with an explicit 30 s SQLite busy timeout, no `init_db()`, no app import, no `os.environ` read; a missing schema exits with an instruction; `codex-lb admin` alone exits non-zero; each command inserts `break_glass_cli_used` (`severity=critical`, `auth_method=cli`) in the same transaction.

## 2. Frontend

- [x] 2.1 Wire contract: widen `LoginHintSchema.localLogin` and `AccessSummarySchema.localLoginPolicy` with `z.enum([...]).catch("enabled")` so an unexpected server value cannot fail the whole session parse, add `breakGlassSession` to `AuthSessionSchema`, `applySession()`, `LEAST_PRIVILEGE_ACCESS` and the `setUnauthorizedHandler` reset, and add a pure `isLocalLoginRequested(search)` helper with its own unit test beside `advanced-settings-deeplink.ts`.
- [x] 2.2 Login surfaces: `/login` as a public route in `isPublicRoute` (already exempt) plus an `App.tsx` route that redirects an authenticated session to `/dashboard`; the local form collapsed behind a reveal link under `admins_only` and rendered only with `local=1` under `break_glass_only`, driven by a prop so `login-form.test.tsx` keeps rendering without a router; the same rule in `PendingIdentityScreen`, which offers the `/login?local=1` link instead of the form under `break_glass_only`.
- [x] 2.3 Login-policy card (`id="organisation-login-policy"`) as the Organisation group's third child, rendered even when there is no reverse-proxy row (restructure `OrganisationGroupBody`'s early return without breaking its "no provider" test): current policy with a control, qualifying state, the "turn on two-factor for `<username>`" hint, the `/login?local=1` URL and the account name, `#organisation-login-policy` deep link, and `settle()` refreshing the session so the tier and the collapsed summary follow.
- [x] 2.4 Error copy and the header: `break_glass_requires_totp` and `last_break_glass_protected` in the organisation and access `EXPLAINED_ERROR_CODES` plus an explained path on the self-service two-factor card (which renders raw server text today); the "Emergency session" indicator as a sibling of the account chip in the actions row and in the mobile sheet.
- [x] 2.5 Mocks and i18n: `local_login_policy` and `breakGlassSession` in `factories.ts`, MSW handlers plus matching `EXPECTED_ENDPOINTS` entries for anything new, and every new key in en, ko and zh-CN (the parity test compares full key sets).

## 3. Verification and docs

- [ ] 3.1 Unit: the qualification predicate over the five facts; `break_glass_only` admitting a qualifying account and refusing a bare designation; the post-state guard for each call site's shape; `isLocalLoginRequested`; the rate-limit key builder (normalization, address pairing, the address-only key, the two limiters' distinct types and ceilings); the scheduler's attempt sweep, including that it runs with sticky cleanup disabled and that its failure does not cost the rest of the pass; the CLI argument parsing, the bare-`admin` exit, the missing-schema message and `reset-password`'s reachability re-open (fires under `break_glass_only` on the last qualifying account, stays out of the way otherwise).
- [ ] 3.2 Integration — sign-in: under `break_glass_only` an ordinary password sign-in answers `401` byte-identically to an unknown username and audits `reason=login_policy`; a break-glass admin must pass two-factor and produces `break_glass_login` at critical severity with an emergency-marked session; unknown and existing usernames stay byte-identical; the break-glass username reaches `429` at the same point an unknown one does (no oracle) while the same account still signs in from another address; one address spraying invented usernames is stopped by the per-address ceiling; a success clears its own per-account bucket and leaves the address ceiling standing, so a valid sign-in cannot reset it; one username limited at one address still signs in from another; the migrated admin without a second factor signs in and enrols at policy `enabled`.
- [ ] 3.3 Integration — guards: zero qualifying accounts refuses a tightening with `409 break_glass_requires_totp` naming the account and admits it after enrolment; relaxing and re-saving are never gated; the provider `enabled` gate and its two audit rows; and, while the policy is not `enabled`, one test per edge on the last qualifying account for removing two-factor, removing the local password, admin reset-totp, disabling, deleting, demoting, clearing the designation and the shared `deactivate_user()` path (`scim_deprovision_refused`), each using a non-compat break-glass admin so the refusal proves the new guard and not `compat_user_locked`; plus the concurrent-race case and the `enabled`-policy no-op case.
- [ ] 3.4 Integration — surface: the trusted-header fallback is unchanged at `enabled` and narrowed at `admins_only`/`break_glass_only`, with the gate and `password_session_active` agreeing in one request; `PUT /api/settings` demands `security:write` and step-up for the new field; the migration's frozen `_PARENT_REVISION` literal reaches head.
- [x] 3.5 Frontend: `/login?local=1` renders the form under `break_glass_only` while `/` does not; a signed-in session at `/login` lands on the dashboard, not the not-found page; the pending screen shows no password field under `break_glass_only`; the login-policy card renders with no reverse-proxy row, shows the hint and the URL, and explains both 409 codes; the emergency indicator appears on the individual tier and clears on sign-out; the collapsed Organisation group still issues zero requests and its one-liner still contains none of the banned words.
- [x] 3.6 `docs/sso.md` (three policy values, **each of** the four commands with its standard-image, distroless and Helm invocation (arguments included), identity-provider-down and lost-secret runbooks, the reverse-proxy two-factor-before-managing-people note, the spec backlink, no realistic secret literals), its `mkdocs.yml` nav entry, and cross-links from `docs/authentication.md`, `docs/deployment/remote.md#reverse-proxy` and `docs/troubleshooting.md`.
- [ ] 3.7 `uv run ruff check . && uv run ruff format --check . && uv run ty check`; `cd frontend && bun run lint && bun run typecheck && bun run test`; `uv run openspec validate local-login-policy-and-break-glass --strict`.
