## 1. Schema (WP-A)

- [x] 1.1 Add `subscription_overflow_source_id` and `subscription_overflow_drain_until` to `DashboardSettings` and the `ModelSourcePin` model with its `purge_at` index.
- [x] 1.2 Add Alembic revision `20260908_000000_add_subscription_overflow` on the current head with guarded, idempotent upgrade and full downgrade.
- [x] 1.3 Cover upgrade/downgrade, idempotent re-run against a partially applied schema, and PostgreSQL invalid-index repair in `tests/integration/test_migrations.py`.
- [x] 1.4 Run migration policy and drift checks on SQLite and PostgreSQL and strict OpenSpec validation.

## 2. Designation settings, preflight, drain deadline, dashboard (WP-B)

- [x] 2.1 Extend this change with the `model-source-routing` designation and preflight requirements.
- [x] 2.2 Settings API: tri-state `subscription_overflow_source_id`, eligibility validation (`400 subscription_overflow_source_invalid`), drain deadline arming/clearing in the same row update, audit `changed_fields`, cross-replica cache invalidation.
- [x] 2.3 `GET /api/settings/subscription-overflow/preflight` (write access; 404 for unknown sources; warnings never block).
- [x] 2.4 Deleting the designated model source clears the designation and arms the drain deadline in the delete's transaction, then invalidates the settings cache.
- [x] 2.5 Dashboard Routing card: designation select (Off sentinel), drain notice showing and gated on the derived pin expiry, inline preflight, help text, i18n en/ko/zh-CN, vitest coverage.
- [x] 2.6 `docs/routing.md` operator explainer linking back to `model-source-routing`.
- [x] 2.7 Inertness proof: request-path/core ratchet unit test plus the end-to-end test that an exhausted pool still answers `429 usage_limit_reached` with a source designated and never contacts it.

## 3. Pins, routing, observability, rollout (WP-C1, WP-C2, WP-D, WP-G)

- [ ] 3.1 Extend this change with the pin repository, overflow routing, observability, and rollout requirements.

### 3.x WP-C1 pure modules and direct source-route hardening (no overflow wiring)

- [x] 3.2 `app/modules/proxy/source_admission.py`: per-source bulkhead enforcing `ModelSource.max_concurrency` (`NULL` unlimited), `SourceAdmission` claims released exactly once by whichever latch holds them (`release_if_unowned` route latch, `release(trial_result)` owner latch), `TrialClaim` hook for the WP-C2 breaker; direct routing claims before the API-key reservation and answers `503 model_source_busy` + `Retry-After: 1` with nothing owned.
- [x] 3.3 `app/modules/proxy/source_dispatch.py`: `SourceDispatch` single latch (`close_source -> settle_or_release -> release_claims + record_result -> write_row`, every step independently latched, failure-isolated, cancellation deferred), `abandon()` cancellation classes (`client_disconnected_during_open`, `source_stall_abandoned`, `client_disconnected_before_body`, `dispatch_interrupted`), `finalize_transport()`, `SourceStreamingResponse`, `open_with_disconnect_watch` (owned open task, 250 ms disconnect poll, 10 s stall evidence, child always cancelled and awaited), `settlement_stream` outermost body layer, settle-at-estimate policy, verified pin-write hook + synthesized `response.created`/`response.failed` pair (primitive only; never armed by direct routing).
- [x] 3.4 `app/modules/proxy/api.py`: `_source_responses_response` rewired onto the owner for limited and unlimited keys (live streaming; the Responses limited-key buffered branch is removed, chat completions keep theirs), telemetry stripped through `strip_source_telemetry`, raced open, non-stream disconnect check, source `Retry-After` merged into pre-open error headers, `context` threaded from both routes for the scheduler/clock/cleanup seams; api.py timing allowances unchanged (19/8).
- [x] 3.5 `app/core/metrics/prometheus.py`: `codex_lb_model_source_dispatch_total{kind,status}`, `_dispatch_abandoned_total{stage}`, `_timeout_total{phase}`, `_bulkhead_rejections_total{source_id}`, `_bulkhead_in_flight{source_id}`, `_usage_estimated_total{source_id,cause}`, `codex_lb_model_source_live_pins{kind}` (registered; sampler lands with WP-C2) in both the real and the stub branch.
- [x] 3.6 Tests: `tests/unit/test_source_admission.py`, `tests/unit/test_source_dispatch.py` (virtual-time watch, latch, settle figures incl. a hypothesis property, synthesized pair, transport finalizer), `tests/integration/test_model_source_dispatch.py` (real stub relay: live limited-key streaming, estimate on missing usage and post-item cancel, release before, non-stream `usage_unavailable`, abandonment cases a-e, `max_concurrency=1` busy, `Retry-After` passthrough, row attribution, telemetry stripped at the stub, static zero-hot-path ratchet); `api-keys` and `proxy-runtime-observability` deltas.
- [x] 3.7 Projection gate: `app/modules/model_sources/projection.py` (`strip_source_telemetry`, `overflow_portability_view`, closed `DeclineReason` set), `app/modules/proxy/replay_safety.py` (`responses_payload_is_provider_portable`, `transcript_is_source_free`, `is_binding_turn_state`; shared predicate hardened to fail closed on malformed nested values), `selection.py::select_overflow_model_source` (designated source only, no registry-precedence skip), `catalog.py::source_model_supports_vision`; synthetic gpt-5.5 / gpt-5.6 Lite fixtures; `responses-api-compat` delta; unit tables per decline reason, order cascade and hypothesis properties.
- [x] 3.8 Transport: `app/modules/model_sources/forwarding.py` (connect 10 s / header 20 s / first-frame 30 s deadlines, idle cap `min(stream_idle_timeout_seconds, 300)` on the scheduler seam, first-chunk open, frame classification and holder observations, pre-content hook with oversized frames counted as content, `retry_after` / `timeout_phase`, Responses-only 401/403 → `502 model_source_credentials_error`, `SourceStreamTransport` close latch), `app/core/clients/http.py` dedicated model-source session (`lease_model_source_session`), `tests/integration/model_source_helpers.py`; `outbound-http-clients` delta; virtual-time and real-socket deadline suites.
- [x] 3.9 Pins and retention: `app/modules/proxy/model_source_pins.py` (thread-only / anchor / bounce keys, drain-capped expiry, dialect repository, positive-only `PinCache`, `lookup_pin_bounded`, `PinWriteExecutor` verified outcomes, `delete_durably`), `app/core/retention/job.py::prune_model_source_pins` + scheduler tick regardless of opt-in, inertness ratchet relaxed for the two pin readers; `model-source-routing` and `data-retention` deltas; SQLite/PostgreSQL parity, drain-cap property, N=50 writer storm.
- [x] 3.10 Exhaustion probe: `app/modules/proxy/_load_balancer/exhaustion_probe.py::probe_pool_usage_exhaustion` (read-only `check_opportunistic_admission(lease_kind=None, service_tier=, observe_only=True)`), `_load_balancer/opportunistic_admission.py` extraction with `apply_lease_release` / `detached_runtime_snapshot`, `_service/support.py::opportunistic_admission_account_scope`; `account-routing` delta; selector-parity hypothesis property (pre-existing drain-strategy divergence pinned as a strict xfail), runtime byte-identity and `service_tier` flip integration tests.
- [x] 3.11 Integration: merge the five packages and `origin/main` into `feat/2123-wp-c1-overflow-core`, count oversized frames as content for the pre-content hook, record decisions 21–28, and run the full gate set (ruff, ty, proxy architecture / cancellation safety / timing seams, strict OpenSpec validation, single Alembic head) with the new, model-source, proxy-responses, api-keys and simulation suites green.
- [x] 3.12 Review fixes (decisions 29–34): admission estimate under the route-helper latch (bulkhead slot never leaks on a raising estimate); EOF tail parsed as a frame before the withheld bytes are released and an aggregate withheld-bytes cap that fails closed; clean EOF without a terminal recorded as `error model_source_stream_truncated` and released while a relayed oversized success terminal stays a success; header / first-frame deadlines armed for Responses streams only (chat streams keep the total-budget-only open); observability delta names the registered `codex_lb_model_source_live_pins` gauge with a two-way drift guard; account-routing parity scenario narrowed to the proven statements with the production-knob property outside the drain strategies.
- [x] 3.13 Review fixes (round 2, decisions 35–38): `settlement_stream`'s cancel handler releases and records `error model_source_response_failed` when a client leaves right after a relayed failure terminal (before the source's EOF) instead of charging the cancel estimate; the stream usage parser joins multi-line `data:` fields through the shared `extract_sse_data` so it and the public wrapper never disagree inside one pipeline; `_source_client_timeout` arms only `sock_connect` (not `connect`, which in aiohttp also bounds the pooled-connection wait) so a saturated dedicated pool queues for a slot instead of failing fast as `model_source_unreachable`, with the `outbound-http-clients` delta stating the pool-wait boundary and a queue-not-unreachable scenario; and behavioural assertions cover the four metric-increment clauses (`…dispatch_total{kind,status}`, `…dispatch_abandoned_total{stage}`, `…bulkhead_rejections_total{source_id}`, `…usage_estimated_total{cause}`) that the `_inc` helper would otherwise let drift silently.
- [x] 3.14 Review fixes (round 4, decisions 39–42): the stream usage parser ignores one leading UTF-8 BOM like the event-block reassembler (BOM-prefixed data-only terminals run the pre-content hook and surface their usage); `settlement_stream`'s completion branch follows the client-visible terminal so a success terminal the public wrapper rewrote into `response.failed` is `error model_source_response_invalid` and released; the cancel estimate keys on `content_delivered` (set at the body's flush point) instead of the parser's output item — delivered delta-only answers are charged, items still withheld ahead of an in-flight pin write are not — and a disconnect right after a relayed success terminal is a `success`; and `probe_pool_usage_exhaustion` takes the caller's settings snapshot and declines under the drain strategies (`codex_lb_pool_exhaustion_probe_declined_total{reason="drain_strategy"}` + reason log), replacing the strict xfail with positive decline tests and a premise test.
- [x] 3.15 Review fixes (round 5, decision 43): delivery is decided by `settlement_stream` from the event frames it hands to the transport (`relayed_frame_delivers_content`, the parser's own `classify_responses_frame`) instead of the body's flush point, which sits below the public wrapper that drops vendor events under the SDK contract, parks `response.*` frames ahead of `response.created` and holds reasoning deltas; the owner no longer mirrors `SourceUsageHolder.content_delivered` (kept as the hook-ordering evidence), so a limited key that leaves after only bookkeeping frames reached the wire is released (api-keys requirement sentence + two scenarios: dropped vendor event, delta parked ahead of `response.created`).
- [x] 3.16 Review fixes (round 6, decisions 44–48): the chat-completions stream open returns at the source's headers as on `main` (the body reads the first token under the total budget alone, so a client that leaves during prompt processing frees the connection, the lease and the reservation; route-level real-disconnect test, ASGI harness shared through `tests/integration/model_source_helpers.py`); every source route relays the source `Retry-After` (`_source_error_response_headers` in the four handlers that answer a pre-open source error -- chat stream open, chat non-stream, embeddings, transcription -- one route-level test per family; the buffered limited-key chat handler only consumes a started body and never observes one, decision 54); the stream usage parser carries a trailing CR across chunks so a CRLF split at a chunk boundary is one line ending; a typeless `{"error": {...}}` record is the `error` failure terminal to the parser and to `_relayed_event_type` through the wrapper's own `classify_event_type`; and `settlement_stream` latches the client-visible terminal (failure sticky) instead of reading the last relayed frame. outbound-http-clients and api-keys deltas updated with the sentences and scenarios.
- [x] 3.17 Review fixes (round 7, decisions 49–50): `_relayed_event_type` reads the `event:` line at the SSE boundary set and a raw block's type through `parse_sse_data_json`, so bare-CR framed and multi-line `data:` frames classify as the client parses them (unit table rows; route-level limited-key cancel after CR-framed `created`/`in_progress` on both Responses paths releases); an empty chat-completions `2xx` stream ends the started body cleanly (`main` parity, no exception out of the ASGI body) while the unlimited row records `error invalid_upstream_response` and the limited-key buffered handler answers `502 invalid_upstream_response` before any byte (unit + two route tests). api-keys and outbound-http-clients deltas updated.
- [x] 3.18 Follow-ups to #2208 (decision 51): the direct chat-completions stream owns its transport (`SourceChatStream.transport`/`aclose()` shared with `SourceResponsesStream`; `SourceStreamingResponse` takes any `TransportOwner`; `SourceChatStreamOwner` closes a never-started body's transport, releases the reservation and writes the `cancelled client_disconnected_before_body` row), with a route-level pre-body abandonment test on the ASGI harness (`stall_response_start`) and unit coverage of the owner latch; outbound-http-clients delta sentence and scenario.
- [x] 3.19 Follow-ups to #2208 (decision 52): `strip_source_telemetry` removes `client_metadata` and `access_programs` whole and only the Codex `reasoning_summary_delivery` key from `stream_options` (object dropped when emptied; `include_obfuscation` and non-object values forwarded); responses-api-compat delta requirement and scenarios, unit table incl. the overflow view declining a surviving `stream_options`.
- [x] 3.20 Follow-ups to #2208 (decision 53): `SourceStreamUsageParser._observe_responses_event` classifies the parser's own parsed frame without a per-frame `dict(event)` copy (micro-benchmark in the decision).
- [x] 3.21 Follow-ups to #2208 (decision 54): the buffered limited-key chat handler's dead `Retry-After` merge is removed with the post-open reasoning at the call site (behaviour unchanged); `test_body_phase_errors_carry_no_retry_after` pins that body-phase forwarding errors carry no `retry_after`; 3.16 / decision 45 wording corrected to four families.
