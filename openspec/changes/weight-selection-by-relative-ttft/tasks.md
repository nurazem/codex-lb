# Tasks

## 1. Sampling and estimators

- [x] 1.1 Add `ttft_cohort.py` with the module constants (1 h window, 64-sample cap, 8 samples / 3 accounts minimum, 20 000-token input cap, effort `None`/`minimal`/`low`, 10% upper trim, 15% deadband, 0.5 floor), `record_ttft_sample` (eligibility filter, bounded append at the balancer clock, lock-free), `account_ttft_estimate_ms` (upper-trimmed mean), `ttft_weight_multiplier` and `fleet_ttft_estimates` (per-account estimates + fleet median over the whole runtime map).
- [x] 1.2 Add `throughput_cohort.py` with the module constants (same window/cap, 8 samples per (account, model), 200 output-token floor, 15% deadband, 0.5 floor), `record_tps_sample` (eligibility: success `normal`, non-empty non-`unknown` model, first token, positive span, >= 200 total output tokens incl. reasoning, no queue wait, single send; any input size / effort), `account_tps_estimate` (median), `tps_weight_multiplier` and `fleet_tps_estimates` (per-model reference, `TTFT_MIN_ACCOUNTS` accounts with evidence for that model, neutral without a model).
- [x] 1.3 Add `latency_cohort.py`: `combine_cohort_multipliers` (minimum, never product; signal label `ttft`/`tps`/`both`/`none`), `last_tps_weight` / `last_cohort_weight`, `apply_latency_cohort_weights(states, runtime, now=, model=, log_transitions=)` with per-signal transition gates (first-token per account, throughput per (account, model), non-neutral throughput weights only).
- [x] 1.4 `RuntimeState.ttft_samples`, `tps_samples` (model -> samples, empty models dropped on prune), `ttft_weight` (last applied first-token multiplier) and `tps_weights` (model -> last applied non-neutral throughput multiplier, key dropped when neutral, map dropped when empty); `detached_runtime_snapshot` deep-copies the containers so observe-only admission cannot write through.

## 2. Wiring

- [x] 2.1 `_write_request_log` records both samples from the funnel arguments (`status`, `request_kind`, `model`, `latency_ms`, TTFT, input / cached input / output tokens, reasoning effort, gate + bridge wait, `upstream_retried`); the WebSocket finalizer sets `upstream_retried` from `response_create_attempt_count > 1` (bridge retries), `replay_count > 0` (transparent direct-WebSocket replays) or a started account-capacity wait; `_acquire_request_state_response_create_admission` folds the global response-create admission wait into `latency_response_create_gate_wait_ms` so a direct WebSocket turn that queued on the saturated global limit is excluded like a bridge-queued one.
- [x] 2.1a Stop the throughput clock at the upstream terminal on every path: the direct WebSocket reader and the HTTP-to-WebSocket bridge reader (`_process_parsed_http_bridge_upstream_event`, right after the terminal request state is popped and before the durable alias / operation / circuit-settlement writes) stamp `_WebSocketRequestState.upstream_terminal_at` when they parse the terminal frame; `_finalize_websocket_request_state` captures it (falling back to its own entry time) before the gate release and the API-key settlement and passes `latency_upstream_terminal_ms` to `_write_request_log`; the HTTP stream stamps `_StreamSettlement.upstream_terminal_at` via `streaming.helpers._stamp_terminal` at both terminal-detection sites of `_stream_once` (before the frame is yielded downstream) and passes `_upstream_terminal_latency_ms(settlement, attempt_started_at)`; `_write_request_log` hands the stamp to `record_tps_sample` in place of the row's `latency_ms`.
- [x] 2.1b Anchor the first-token sample at the upstream send: `_write_request_log` takes `latency_upstream_send_ms` (start-to-`response.create`-send offset, not persisted) and `record_ttft_sample` records `latency_first_token_ms - latency_upstream_send_ms`, dropping rows without an anchor or with a send after the first token; the WebSocket/bridge finalizer derives the offset from `response_create_sent_at`, the HTTP stream passes `0` (its attempt clock is re-anchored right before the send).
- [x] 2.2 `_build_states(model=...)` applies `apply_latency_cohort_weights` over the full runtime map after the states are built, compounding with the error-rate multiplier; `_prepare_sticky_selection_states(model=...)` passes it through.
- [x] 2.3 Thread the requested model from `select_account` into `UnboundSelectionRequest.model` / `StickySelectionRequest.model` and from `OpportunisticAdmissionRequest.model` into both the live and the observe-only (`_observe_selection_states`) builds; extend the `StatesBuilder`, `StickySelectionOwner` and `OpportunisticAdmissionOwner` protocols. `load_balancer.py` 3009/3021 (the body of `_prepare_sticky_selection_states` moved to `sticky_selection.prepare_selection_states`; the method and the `_build_states` seam stay on the balancer), `select_account` 490/527.
- [x] 2.4 Transition-gated `latency_cohort_weight_change` INFO line naming the signal, the model and which signal transitioned (`changed=`), without account identifiers; the first-token gate is per account (one TTFT transition is one line across models), the throughput gate per (account, model); observe-only snapshot builds pass `log_weight_transitions=False` so the line is not repeated.

## 3. Documentation

- [x] 3.1 `docs/routing.md`: "Relative latency weighting" paragraph after "Prefer earlier reset" covering both signals and the minimum rule.
- [x] 3.2 `openspec/specs/account-routing/context.md`: rationale section (fleet-relative, no setting, 0.5 floor, trimmed mean for TTFT, median for throughput, per-model reference, minimum combination, `routing_policy=preserve` alternative, production evidence).

## 4. Verification

- [x] 4.1 Unit tests. TTFT (`tests/unit/test_ttft_cohort_weighting.py`): eligibility filter per request kind / status / effort / input / queued wait, age and cap pruning, retried / capacity-wait rows and the uncached-input cap, trimmed-mean estimator on a bimodal mixture and a 30 s stall, deadband and floor, thin fleet evidence neutral, uniformly slow fleet neutral, prod-shaped cohorts, compounding with the error-rate multiplier, balancer-level steering and lift after the window clears, established `prompt_cache` owner kept and every deterministic strategy unchanged, full-runtime fleet reference under narrowed selection and empty-runtime neutrality, request-log funnel records only eligible rows, detached snapshot copy, transition log gating and identifier hygiene, no duplicate log from a detached-snapshot build. Throughput (`tests/unit/test_throughput_cohort_weighting.py`): eligibility filter incl. model `None`/`""`/`unknown`, output-token floor, non-positive span, queued and retried rows; per-model age/cap pruning and empty-model cleanup; median estimator robust to a stall and a burst, neutral below 8 samples / on another model / without a model; deadband and floor; per-model isolation (slow on sol, neutral on astra and on a model without evidence); prod-shaped sol/astra cohorts (fleet 66 tok/s, slow cohort ~0.6, astra neutral); uniform fleet neutral; neutral below 3 accounts for the model (astra-only evidence does not count); combination rule (`min`, signal label, no compounding, error-rate still compounds); balancer-level steering by requested model with lift after the window; `_build_states` model threading and full-runtime reference; request-log funnel records a 40 tok/s sample for a qualifying 60k-input high-effort row (no TTFT sample) and nothing for queued / gated / replayed / short / model-less rows; neutral builds for unknown / evidence-less / model-less requests leave `tps_weights` empty and the key is dropped when the discount lifts; sticky fresh draw (`select_account(sticky_key=, sticky_kind=, model=)` with no owner) records the sol weight and not an astra one; opportunistic admission observe-only builds for the requested model on the detached snapshot without touching the live runtime, the live check records the weight and picks the same account; detached snapshot copies `tps_samples`, `tps_weights` and `ttft_weight`; snapshot build writes the snapshot weight, not the live one; transition log names `signal=tps model=... changed=tps`, carries no ids, is gated per model across alternating sol/astra builds and logs the lift once; a TTFT-only transition is logged once across sol / astra / model-less / unknown-model builds and allocates no model key, and a later throughput discount under a dominating TTFT floor is logged as `changed=tps`. WebSocket path (`tests/unit/test_websocket_ttft_sample_eligibility.py`, `tests/unit/test_websocket_upstream_transport_observability.py`): finalizer marks replayed and multi-attempt turns as retried, admission wait recorded into the gate wait and both kept out of the window; a keyed WebSocket turn whose settlement takes 10 s records 40 tok/s (not 20) through the real finalizer and funnel, with and without the reader's terminal stamp, while the persisted row keeps the 22 s wall latency; a bridge turn with 3 s of pre-send work and a 4.7 s wall TTFT records a 1.7 s sample (row keeps 4.7 s), a turn without a send stamp records none, a direct turn's 200 ms owner binding is dropped; a bridge turn stamped at 12 s with 10 s of bookkeeping before the finalizer and a 10 s settlement records 40 tok/s while the row keeps 32 s. Bridge reader (`tests/unit/test_proxy_http_bridge.py`): `_process_http_bridge_upstream_text` stamps `upstream_terminal_at` on the matched request state when it parses `response.completed`, 10 s before the (stubbed) finalizer is entered. HTTP stream path (`tests/unit/test_http_stream_latency_cohort_samples.py`): the real `_stream_once` through the real funnel, a 5 s downstream drain of the terminal frame plus a 5 s lingering upstream, records 40 tok/s and a 2000 ms first-token sample while the row keeps 22 s; an upstream that ends without a terminal frame writes a `stream_incomplete` error row and records nothing. Transition gate: a move of exactly `_LOG_DELTA` is logged.
- [x] 4.2 ruff, ty, proxy architecture / cancellation-safety / timing-seam / settings-tier guards (Settings stays 130, `load_balancer.py` at 3009/3021), strict OpenSpec validation, regression files for the touched modules.
