# Tasks

## 1. Selection

- [x] 1.1 `StickySelectionRequest.exclude_account_ids: frozenset[str] = frozenset()` (defaulted; the only constructor is `LoadBalancer.select_account`).
- [x] 1.2 `LoadBalancer.select_account` forwards `frozenset(excluded_ids)` into the sticky request (one line; `load_balancer.py` and the `select_account` span stay under their ceilings).
- [x] 1.3 `run_sticky_selection_path`: after the bare-session `preserve_existing_mapping` expression, extend it for a soft TTL-bounded, non-hard, non-reallocated, non-isolated owner absent from `selection_states` when it is either present in the pre-cap `states` (capped) or in `request.exclude_account_ids` while in `effective_continuity_owner_candidates` with a status in `_RECOVERABLE_STATUSES` (excluded by this request). Everything downstream is existing code (`persist_fallback=False`, no delete on the `pinned is None` branch, `internal_soft_affinity_spillover` log, no mutation persisted).
- [x] 1.4 `internal_soft_affinity_spillover` honours `redact_sensitive_details`: the flag is threaded through the `_select_with_stickiness` Protocol, `LoadBalancer._select_with_stickiness` and the core function (one defaulted kwarg each); both account ids print as `<redacted>` when requested.

## 2. Verification

- [x] 2.1 `tests/unit/test_load_balancer_concurrency.py`: capped prompt-cache owner (stream cap 8 and response-create cap 4) spills without rebinding and is reused once the cap clears; owner excluded by the retry loop spills without rebinding and is reused next turn, for `active`, `rate_limited`, `quota_exceeded` and `reauth_required` owners; `select_account(exclude_account_ids=…)` reaches `StickySelectionRequest.exclude_account_ids` (plumbing boundary); security-work-filtered owner, paused owner, deactivated owner, owner not in the pool, isolated-and-capped owner, and `reallocate_sticky` are all still rebound; bare `codex_session` (no TTL) excluded owner still persists its fallback; the spillover log prints `<redacted>` ids when the request asks for redaction.
- [x] 2.2 `tests/unit/test_proxy_soft_sticky_spillover.py` (new, uncapped): the real HTTP streaming retry loop drives the real `LoadBalancer` over stub repositories with a `thread-id` header. A code-less 429 on the prompt-cache owner fails over to the alternate: the reselect carries `exclude_account_ids={owner}`, `sticky_kind=prompt_cache`, a non-`None` `sticky_max_age_seconds` and `reallocate_sticky=False`; no sticky row is written or deleted by the failover selection, the mapping still names the owner, `internal_soft_affinity_spillover` is logged, and the next turn returns to the owner. A sibling drives the account/model rejection site (`reallocate_sticky=True`) and asserts the alternate is rebound as before.
- [x] 2.3 Regression: `uv run pytest tests/unit/test_proxy_soft_sticky_spillover.py tests/unit/test_select_with_stickiness.py tests/unit/test_load_balancer_concurrency.py tests/unit/test_overload_backoff.py tests/unit/test_load_balancer_contract.py tests/unit/test_proxy_load_balancer_refresh.py tests/unit/test_proxy_http_bridge.py tests/unit/test_proxy_utils.py`.
- [x] 2.4 Guards: `ruff check`, `ruff format --check`, `check_proxy_architecture.py`, `check_cancellation_safety.py`, `check_proxy_timing_seams.py`, `check_settings_tiers.py` (130/130 unchanged), `ty check`, `openspec validate --specs` and `openspec validate preserve-soft-sticky-owner-on-request-local-unavailability --strict`.
